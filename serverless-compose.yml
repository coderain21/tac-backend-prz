services:
  buyers:
    path: services/buyers
    config: ${env:config}
  bids:
    path: services/bids
    config: ${env:config}
  subdomain:
    path: services/subdomain
    config: ${env:config}
  address-management:
    path: services/address-management
    config: ${env:config}
  cart-management:
    path: services/cart-management
    config: ${env:config}
  buyer-wishlist:
    path: services/buyer-wishlist
    config: ${env:config}
  payments:
    path: services/payments
    config: ${env:config}
  order-management:
    path: services/order-management
    config: ${env:config}
  admin-management:
    path: services/admin-management
    config: ${env:config}
  lot-bid-history:
    path: services/lot-bid-history
    config: ${env:config}
  admin-buyer-management:
    path: services/admin-buyer-management
    config: ${env:config}
  seller-bidder-management:
    path: services/seller-bidder-management
    config: ${env:config}
  # bdd-api:
  #   path: services/bdd-api
  #   config: ${env:config}
  site-banner:
    path: services/site-banner
    config: ${env:config}
  users:
    path: services/users
    config: ${env:config}
  auctions:
    path: services/auctions
    config: ${env:config}
