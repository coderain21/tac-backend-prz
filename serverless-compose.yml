services:
  users:
    path: services/users
    config: ${env:config}
  auctions:
    path: services/auctions
    config: ${env:config}
  buyers:
    path: services/buyers
    config: ${env:config}

    