dev:
  MONGODB_CONNECTION_STRING: ${ssm:MONGODB_CONNECTION_STRING}
qa:
  MONGODB_CONNECTION_STRING: ${ssm:MONGODB_CONNECTION_STRING}
pre-production:
  MONGODB_CONNECTION_STRING: ${ssm:MONGODB_CONNECTION_STRING}
prod:
  MONGODB_CONNECTION_STRING: ${ssm:MONGOBETWEEN_CONNECTION_STRING}